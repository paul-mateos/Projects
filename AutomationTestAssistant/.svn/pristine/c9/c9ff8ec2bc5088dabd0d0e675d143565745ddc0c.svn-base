using System.Collections.Generic;
using System.Collections.ObjectModel;

namespace AutomationTestAssistantDesktopApp
{
    public static class ObservableCollectionManager
    {
        //public static ObservableCollection<T> ToObservableCollection<T, N>(this IEnumerable<N> collection) where T : N
        //{
        //    ObservableCollection<T> obsCollection = new ObservableCollection<T>();
        //    foreach (N item in collection)
        //    {
        //        obsCollection.Add(item);
        //    }
        //    return obsCollection;
        //}
    }
}
